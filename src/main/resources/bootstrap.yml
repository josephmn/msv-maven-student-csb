spring:
  cloud:
    azure:
      appconfiguration:
        stores:
          - connection-string: ${AZURE_APP_CONFIGURATION_CONNECTION_STRING}
#            selects:
#              - key-filter: /application/
#                label-filter: ${spring.profiles.active:dev}